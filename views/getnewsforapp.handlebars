<h1>martinn6's Valve GetNewForApp API Example</h1>
<form method="post">
	<fieldset>
		<legend>Use my Valve info or user your own</legend>
		Your API Key: <input type="text" name="yourAPIKey" id="yourAPIKey" value = "{{yourAPIKey}}" size="42"><BR>
		Valve AppId: <input type="text" name="valveUserID" id="valveAppID" value = "{{valveAppID}}"><BR>
		Max Count: <input type="text" name="maxCount" id="maxCount" value = "{{maxCount}}"><BR>
		<input type="submit" name="formSubmit" value="Submit">
	</fieldset>
</form>
<BR>
{{#if games}}
<h3>Number of Games:</h3>
<span>{{numberofgames}}</span>
<BR>
<h3>List of Games:</h3>
	{{#each games}}
	<div>{{#if this.name}}Name: {{this.name}};{{/if}} ID: {{this.appid}}</div>
	{{/each}}
{{/if}}

